package com.eknox.moneytransfer.enums;
// XOF = FCFA dans le système international.
// Pour un usage ultérieur, cette classe devra être remplacée par une API fournissant l'ensemble des devises.
// Cette mise à jour, sera biensûr fonction de l'offre de devise fournie par l'application, le devise par
// défaut étant le FCFA.
public enum Devise {
    USD, EUR, GBP, CAD, AUD, JPY, HKD, NZD, XOF;
}
